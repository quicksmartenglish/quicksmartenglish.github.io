source 'https://rubygems.org'

gem 'jekyll', '~> 3.9'
gem 'jekyll-paginate' # Add this line for pagination
gem 'kramdown', '~> 2.3.0' # Specify the version based on your needs
gem 'kramdown-parser-gfm', '~> 1.1.0' # Specify the version based on your needs
gem 'csv' # Add csv to address the first warning
gem 'base64' # Add base64 to address the second warning
gem 'bigdecimal' # Add bigdecimal to address the third warning
gem 'webrick' # Add webrick to address the last error
gem 'rouge', '~> 3.28' # Add this line for syntax highlighting

# Add other dependencies as needed
